"use strict";

// write cone in here
const appleButton = document.getElementById("apple");
const textElement = document.getElementById("text").innerHTML;
console.log(textElement);
function changeText(text) {
  textElement.textContent = text;
}
//evaluation strategy
// evaluate all argument from left to Right
